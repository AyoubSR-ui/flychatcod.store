[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @workspace/flychat run build"]

[start]
cmd = "node_modules/.bin/vite preview --config artifacts/flychat/vite.config.ts --host 0.0.0.0 --port 4173"
