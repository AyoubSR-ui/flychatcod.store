[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @workspace/flychat run build"]

[start]
cmd = "artifacts/flychat/node_modules/.bin/vite preview --config artifacts/flychat/vite.config.ts --outDir artifacts/flychat/dist --host 0.0.0.0 --port 4173"
